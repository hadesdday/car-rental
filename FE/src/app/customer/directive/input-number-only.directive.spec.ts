import { InputNumberOnlyDirective } from './input-number-only.directive';

describe('InputNumberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new InputNumberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
